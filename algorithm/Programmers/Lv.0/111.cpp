#include <string>
#include <vector>

using namespace std;

int solution(vector<int> numbers, int k) {
    int answer = 0;
    int first = 0;
    int n = numbers.size();

    while (n >1)
    {
        int skip = (k - 1) % n;
    }


    return answer;
}

int main()
{
    solution({ 1,2,3,4,5,6}, 5);
}