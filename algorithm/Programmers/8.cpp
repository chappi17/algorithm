#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <math.h>
using namespace std;
// 제곱수 판별하기
int main()
{
	int n = 80;
	int temp = 0;
	int answer = 0;

	if (temp = sqrt(n))
	{
		if (temp* temp == n)
		{
			answer = 1;
		}
		else
			answer = 2;
	}

	return 0;
}
