#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
using namespace std;


int main()
{
	int w, h, n,l,d,x,y;
	cin >> w >> h;
	cin >> n;
	vector<int> grid(w*h, 0);

	//for (int i = 0; i < n; i++)
	//{
	//	cin >> l >> d >> x >> y;
	//	grid[x][y] = { 1 };
	//}

	//for (int i = 0; i < w; i++)
	//{
	//	for (int j = 0; j < h; j++)
	//	{
	//		cout << grid[x][y] << " ";
	//	}
	//	cout << endl;
	//}


}